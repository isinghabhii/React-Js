function Chai(){
    return(
        <h3>chai is ready</h3>
    )
}

export default Chai

// Function name always start from capital letter

// when making components, always capitalize name(function) eg: Chai.
// file extension .jsx , bcz some libraries force us for that
// A component can only return a single element (which may contain multiple child elements and components).
// So yeah technically, we can return multiple components, but those should be wrapped by a single parent element.